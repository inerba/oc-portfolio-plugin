# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    name:
        label: prismify.portfolio::lang.models.industry.columns.name.label
        searchable: true

    project_count:
        label: prismify.portfolio::lang.models.industry.columns.project_count.label
        sortable: false

    created_at:
        label: prismify.portfolio::lang.models.all.columns.created_at.label
        type: date

    updated_at:
        label: prismify.portfolio::lang.models.all.columns.updated_at.label
        type: date