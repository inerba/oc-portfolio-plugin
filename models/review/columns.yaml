# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    author:
        label: prismify.portfolio::lang.models.review.columns.author.label
        select: concat(author_name, ' (', author_position, ')')
    created_at:
        label: prismify.portfolio::lang.models.all.columns.created_at.label
        type: date
        invisible: true

    updated_at:
        label: prismify.portfolio::lang.models.all.columns.updated_at.label
        type: date
        invisible: true

    published_at:
        label: prismify.portfolio::lang.models.all.columns.published_at.label
        type: date