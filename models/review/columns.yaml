# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    title:
        label: prismify.portfolio::lang.models.review.columns.title.label
        searchable: true

    created_at:
        label: prismify.portfolio::lang.models.all.columns.created_at.label
        type: date
        invisible: true

    updated_at:
        label: prismify.portfolio::lang.models.all.columns.updated_at.label
        type: date
        invisible: true

    published_at:
        label: prismify.portfolio::lang.models.all.columns.published_at.label
        type: date